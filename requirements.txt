Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.2.1
sqlcipher3-wheels==0.5.4
python-dotenv==1.0.1
reportlab==4.2.5
openpyxl==3.1.5
Pillow==10.4.0
pywin32==312 ; sys_platform == "win32"

# Déploiement "fenêtre native" (§2.1, §15.1 spec) : desktop.py fait tourner
# l'app via Waitress dans une fenêtre pywebview, compilée en .exe par
# PyInstaller (build/packaging uniquement, pas requis pour lancer l'app en
# développement via `python run.py`).
pywebview==5.3.2
waitress==3.0.2
pyinstaller==6.21.0

pytest==8.3.3
pytest-flask==1.3.0
